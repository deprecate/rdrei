[general]
application.name = ${appname}
application.version = 0.1
cache.enabled = ${debug_enabled}

[database]
sqlalchemy.url = sqlite:///%(here)s.sqlite

[locations]
static = %(here)s/${appname}/static

